using BeatSaberMarkupLanguage.Attributes;
using BeatSaberMarkupLanguage.ViewControllers;

namespace AutoBS.UI
{
    /*
    [HotReload]
    [ViewDefinition("AutoBS.UI.WallSettings.bsml")]
    internal class WallSettingsViewController : BSMLAutomaticViewController
    {
        // If you need to add any [UIAction]s or [UIValue] bindings in the wall settings page, do it here.
        // For example, if you want to use a back button within the BSML itself, you can add:
        //[UIAction("BackToMainSettings")]
        //private void BackToMainSettings()
        //{
        //    // You can leave this empty if you want to use the native FlowCoordinator back button.
        //}
    }
    */
}
